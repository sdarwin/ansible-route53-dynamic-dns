---
warn_list:  # or 'skip_list' to silence them completely
  - internal-error  # Unexpected internal error.
skip_list:
  - internal-error
  # - fqcn-builtins  # Use FQCN for builtin actions. Remove this later.
